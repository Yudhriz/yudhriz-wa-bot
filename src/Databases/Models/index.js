const Menu = require("./Menu");
const Session = require("./Session");
const State = require("./State");

// Mengumpulkan semua model dalam objek
const models = {
  Menu,
  State,
  Session,
};

module.exports = models;
